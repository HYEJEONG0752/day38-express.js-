<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시글 목록</title>

</head>
<body>
    <h1>게시글 목록</h1>
    <ul>
        <% posts.forEach(post => { %>
            <li>
                <a href="/posts/<%= post.id %>"><%= post.title %></a>
                <br>
                <small>작성일: <%= post.createdAt.toLocaleString() %></small>
                <!-- 게시글 수정 버튼 -->
                <a class = "수정" href="/posts/<%= post.id %>/edit">수정</a>
                <!-- 게시글 삭제 버튼 -->
                <form action="/posts/<%= post.id %>?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit">삭제</button>
                </form>
            </li>
        <% }) %>
    </ul>
    <a
